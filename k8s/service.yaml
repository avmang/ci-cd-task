apiVersion: v1
kind: Service
metadata:
  name: mavoyan-flask-app
  labels:
    app: mavoyan-flask-app
spec:
  type: LoadBalancer
  selector:
    app: mavoyan-flask-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None
